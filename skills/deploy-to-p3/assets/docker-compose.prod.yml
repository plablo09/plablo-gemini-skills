services:
  app:
    image: ${IMAGE}
    container_name: {{APP_NAME}}-app
    restart: always
    expose:
      - "{{PORT}}"
    {{ENV_VARS_BLOCK}}
    networks:
      - gateway

networks:
  gateway:
    external: true
